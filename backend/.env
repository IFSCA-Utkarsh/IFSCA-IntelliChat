# ─────────────────────────────────────────────────────────────
# IFSCA IntelliChat – Optimized Production .env (2025)
# ─────────────────────────────────────────────────────────────

# === Vector DB (Chroma – replaces FAISS) ===
CHROMA_PERSIST_DIR=backend/data/chroma_db
COLLECTION_NAME=ifsca_index

# === Embedding & Reranking Models ===
EMBED_MODEL=intfloat/e5-large                    # Your excellent choice – kept!
RERANK_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2 # Best accuracy/speed trade-off

# === LLM ===
LLM_MODEL=ifsca_utkarsh_rag_assistant             # Your fine-tuned model – kept!
OLLAMA_HOST=http://127.0.0.1:11434

# === GPU Acceleration (set True if you have NVIDIA GPU + CUDA) ===
USE_GPU_FOR_EMBEDDINGS=True                       # ← Change to False if no GPU

# === Dual PDF Folders (exactly as you requested) ===
RAG_DATA_DIR=data/RAGData                         # Folder used for embedding & chunking
RAG_DATA_DIR2=data/RAGData2                       # Folder used for clean PDF download/serving

# === Chunking & Retrieval Settings (optimized) ===
CHUNK_SIZE=2000
CHUNK_OVERLAP=300
INITIAL_K=20                                      # Retrieve 20 docs first (for reranking)
TOP_K=5                                           # Final top-5 after reranking (shown to user)

# === Server ===
HOST=0.0.0.0
PORT=8000

# === Auth ===
JWT_SECRET=3862F6EBC1F37B29AF4C8850B261B6148242FC91518DD2F9E92FBE32E07F9057
JWT_EXPIRE_MIN=120


LOG_LEVEL=INFO